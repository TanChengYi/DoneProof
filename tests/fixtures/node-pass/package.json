{
  "name": "doneproof-node-fixture",
  "private": true,
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "lint": "eslint .",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  }
}
